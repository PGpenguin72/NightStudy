{
  "name": "nightstudy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute nightstudy --local --file=migrations/0001_schema.sql",
    "db:init:remote": "wrangler d1 execute nightstudy --remote --file=migrations/0001_schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.5.2",
    "wrangler": "^3.78.7"
  }
}
